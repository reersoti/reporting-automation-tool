# reporting-automation-tool
A user launches the desktop application and:  selects source data files;  chooses output location;  sets report parameters;  runs report generation;  receives a ready-to-use Excel report.  This reduces repetitive manual work and makes the reporting process more consistent.
